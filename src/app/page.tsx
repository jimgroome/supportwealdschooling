import { Grid, Typography } from "@mui/material";

const Home = () => (
  <Grid container>
    <Grid item xs={12}>
      <Typography variant="body1">Petition coming soon.</Typography>
    </Grid>
  </Grid>
);

export default Home;
