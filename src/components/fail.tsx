import { Typography } from "@mui/material";

const Fail = () => (
  <Typography variant="body1">
    Sorry, that didn&rsquo;t work. Have you already signed the petition?
  </Typography>
);

export default Fail;
